{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('admin_createRecipeReact') }}
{% endblock %}

{%  block body %}
    <div data-recipeId="{{ recipeId }}" data-diets="{{ diets }}" data-allergens="{{ allergens }}" id="reactForm"></div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('admin_createRecipe') }}
{% endblock %}